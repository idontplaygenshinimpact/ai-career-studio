# AI Career Studio 模拟面试复盘

综合评分：85/100

面试状态：已结束
已记录回答：17 轮
当前追问点：项目二：仿网易云音乐
当前点追问深度：0

## 总体建议
项目理解还不错，但对异常、性能和重构思路的表达可以更完整。

## 分项评分
- 技术准确性：28
- 表达结构：18
- 项目深度：25
- 异常边界：12
- 复盘意识：15

## 逻辑边界说明
本次模拟面试不设置固定题量上限；系统会围绕当前技术点持续追问，直到该点已问透，或继续追问会超出简历与技术点的合理边界。

## 完整追问轨迹
### 追问 1：简历总览
- 问题：先不看细节，结合这份简历，你想让面试官第一时间记住你的哪三件事？为什么？
- 考察维度：个人定位、能力主线、求职目标
- 触发依据：当前追问点：简历总览。边界：只追问候选人的能力主线、项目真实性、求职定位和简历表达，不展开到某个项目的底层实现细节。
- 合格标准：合格回答需要覆盖背景、个人动作、技术选择、结果验证；追问可以继续，但必须严格控制在该追问点的逻辑边界内：只追问候选人的能力主线、项目真实性、求职定位和简历表达，不展开到某个项目的底层实现细节。
- 我的回答：我希望面试官记住三点：第一，我的方向是前端工程和 AI 应用前端结合；第二，我做过 AgentChat、音乐播放器、电商平台这些覆盖 Vue、React、TypeScript、状态管理、流式交互的项目；第三，我在实习中参与过 img2code 数据生产链路和标注工具，能理解真实协作、质量校验和流程闭环。我的差异化不是只写静态页面，而是能把复杂交互、工程化配置和业务流程一起考虑。

### 追问 2：基础：HTTP 状态码与缓存
- 问题：先插一个牛客高频八股：HTTP 常见状态码有哪些？304 和 200 from cache 的区别是什么？强缓存和协商缓存分别看哪些 Header？
- 考察维度：HTTP 状态码、强缓存、协商缓存、304、资源缓存策略
- 触发依据：当前追问点：基础：HTTP 状态码与缓存。边界：只围绕前端实习高频 HTTP 状态码、304、强缓存/协商缓存、Cache-Control、Expires、ETag、Last-Modified、静态资源缓存策略追问，不扩展到服务端缓存集群实现。
- 合格标准：合格回答需要覆盖背景、个人动作、技术选择、结果验证；追问可以继续，但必须严格控制在该追问点的逻辑边界内：只围绕前端实习高频 HTTP 状态码、304、强缓存/协商缓存、Cache-Control、Expires、ETag、Last-Modified、静态资源缓存策略追问，不扩展到服务端缓存集群实现。
- 我的回答：常见状态码里 200 表示成功，301/302 是重定向，304 表示协商缓存命中，401 是未认证，403 是无权限，404 是资源不存在，500 是服务端错误。强缓存主要看 Cache-Control 和 Expires，命中时可能是 200 from disk cache 或 memory cache；协商缓存会带 If-None-Match 或 If-Modified-Since 到服务端，服务端判断资源没变返回 304，所以 304 不是错误，而是告诉浏览器复用本地缓存。ETag 精度比 Last-Modified 高，但会有生成成本；静态资源通常用内容 hash 配合长缓存。

### 追问 3：基础：URL 到页面展示
- 问题：从浏览器输入 URL 到页面展示，完整链路是什么？请按网络请求和浏览器渲染两段讲。
- 考察维度：DNS、TCP、TLS、HTTP、DOM/CSSOM、Layout/Paint/Composite
- 触发依据：当前追问点：基础：URL 到页面展示。边界：只围绕 URL 解析、DNS、TCP 三次握手、TLS、HTTP 请求响应、HTML/CSS/JS 解析、DOM/CSSOM/渲染树、回流重绘合成追问，不深入浏览器 C++ 内核源码。
- 合格标准：合格回答需要覆盖背景、个人动作、技术选择、结果验证；追问可以继续，但必须严格控制在该追问点的逻辑边界内：只围绕 URL 解析、DNS、TCP 三次握手、TLS、HTTP 请求响应、HTML/CSS/JS 解析、DOM/CSSOM/渲染树、回流重绘合成追问，不深入浏览器 C++ 内核源码。
- 我的回答：输入 URL 后，浏览器先解析 URL，查找 DNS 缓存并进行域名解析，随后建立 TCP 连接，HTTPS 还要做 TLS 握手，再发送 HTTP 请求并接收响应。拿到 HTML 后构建 DOM，解析 CSS 生成 CSSOM，合成 Render Tree，然后经历 Layout、Paint、Composite 展示页面。script 默认会阻塞 HTML 解析，defer 会等 DOM 解析后按顺序执行，async 下载完就执行，适合独立脚本。性能排查时我会分网络耗时、资源大小、主线程阻塞和渲染次数来看。

### 追问 4：基础：TCP / UDP / HTTPS
- 问题：牛客字节、美团面经里经常问：TCP 和 UDP 有什么区别？TCP 为什么要三次握手、四次挥手？HTTPS 的 TLS 握手大致做了什么？
- 考察维度：TCP UDP 区别、三次握手四次挥手、可靠传输、TLS 加密
- 触发依据：当前追问点：基础：TCP / UDP / HTTPS。边界：只围绕 TCP/UDP 基础差异、可靠传输、三次握手四次挥手、HTTPS/TLS 基本流程、对称/非对称加密职责追问，不深入拥塞控制公式或内核实现。
- 合格标准：合格回答需要覆盖背景、个人动作、技术选择、结果验证；追问可以继续，但必须严格控制在该追问点的逻辑边界内：只围绕 TCP/UDP 基础差异、可靠传输、三次握手四次挥手、HTTPS/TLS 基本流程、对称/非对称加密职责追问，不深入拥塞控制公式或内核实现。
- 我的回答：TCP 是面向连接、可靠、有序的传输协议，适合网页请求和文件传输；UDP 无连接、开销小、实时性好，适合直播、语音、游戏等场景。三次握手是为了确认双方收发能力和初始化序列号，四次挥手是因为 TCP 双向通道要分别关闭。HTTPS 在 TCP 后增加 TLS 握手，用非对称加密协商密钥和验证证书，再用对称加密传输数据，这样兼顾安全和性能。HTTP/2 主要是多路复用和头部压缩，HTTP/3 基于 QUIC/UDP，减少队头阻塞和连接迁移成本。

### 追问 5：项目一：AgentChat
- 问题：AgentChat 这个项目里，除了 SSE 之外，你觉得最难的一块是什么？为什么它比单纯的页面开发更考验工程能力？
- 考察维度：流式通信、状态管理、消息一致性
- 触发依据：当前追问点：项目一：AgentChat。边界：只围绕多模型对话、SSE 流式输出、Pinia 状态拆分、多会话消息一致性、Markdown 渲染、数据看板和前端工程化追问，不追问未在简历中声明的后端 LangChain 深层实现。
- 合格标准：合格回答需要覆盖背景、个人动作、技术选择、结果验证；追问可以继续，但必须严格控制在该追问点的逻辑边界内：只围绕多模型对话、SSE 流式输出、Pinia 状态拆分、多会话消息一致性、Markdown 渲染、数据看板和前端工程化追问，不追问未在简历中声明的后端 LangChain 深层实现。
- 我的回答：AgentChat 里除了 SSE，最难的是多会话消息一致性和状态管理。我的设计会把会话列表、当前会话、消息流状态、模型配置拆到 Pinia store 中；每条 assistant 消息有 messageId、sessionId、status、content、error 等字段。SSE chunk 到来时只追加到对应 sessionId/messageId，用户切换会话不会影响后台流。取消时用 AbortController，并把状态从 streaming 变成 aborted；重试时新建一条 assistant 消息或复用明确的 retryId，避免内容串台。Markdown 流式渲染会节流更新，代码高亮延后或按块处理。

### 追问 6：基础：JavaScript 类型与原型链
- 问题：JS 基本数据类型有哪些？typeof null 为什么是 object？判断数组有哪些方式？原型链和 instanceof 的原理是什么？
- 考察维度：JS 数据类型、类型判断、数组判断、原型链、new、instanceof
- 触发依据：当前追问点：基础：JavaScript 类型与原型链。边界：只围绕 JS 类型、typeof、instanceof、Object.prototype.toString、Array.isArray、原型链、new 操作符、构造函数与 prototype 追问，不深入 JS 引擎源码。
- 合格标准：合格回答需要覆盖背景、个人动作、技术选择、结果验证；追问可以继续，但必须严格控制在该追问点的逻辑边界内：只围绕 JS 类型、typeof、instanceof、Object.prototype.toString、Array.isArray、原型链、new 操作符、构造函数与 prototype 追问，不深入 JS 引擎源码。
- 我的回答：JS 基本类型包括 string、number、boolean、undefined、symbol、bigint、null，引用类型主要是 object。typeof null 是 object 是历史遗留的二进制标记问题。判断数组可以用 Array.isArray，Object.prototype.toString.call 也比较稳，instanceof 会受多 realm 或 iframe 影响。原型链是对象通过 [[Prototype]] 向上查找属性，instanceof 本质是判断构造函数的 prototype 是否出现在对象原型链上。new 的过程是创建对象、绑定原型、执行构造函数并绑定 this，如果构造函数返回对象则返回该对象，否则返回新对象。

### 追问 7：基础：事件循环与 Promise
- 问题：讲一下浏览器事件循环：宏任务和微任务分别有哪些？Promise、async/await、setTimeout 的执行顺序怎么判断？
- 考察维度：宏任务、微任务、Promise、async/await、请求乱序
- 触发依据：当前追问点：基础：事件循环与 Promise。边界：只围绕浏览器事件循环、宏任务、微任务、Promise then/catch/finally、async/await、setTimeout、requestAnimationFrame、异步请求乱序追问，不扩展到 Node.js libuv 深层实现。
- 合格标准：合格回答需要覆盖背景、个人动作、技术选择、结果验证；追问可以继续，但必须严格控制在该追问点的逻辑边界内：只围绕浏览器事件循环、宏任务、微任务、Promise then/catch/finally、async/await、setTimeout、requestAnimationFrame、异步请求乱序追问，不扩展到 Node.js libuv 深层实现。
- 我的回答：浏览器事件循环里同步代码先执行，宏任务包括 script、setTimeout、setInterval、I/O、UI 事件等，微任务包括 Promise.then、queueMicrotask、MutationObserver。每轮宏任务结束后会清空微任务队列，再进入渲染或下一个宏任务。async/await 本质上会把 await 后面的逻辑放到微任务中继续执行。请求乱序时我会用请求序号、AbortController 或只接受最后一次请求结果来避免旧响应覆盖新状态。try/catch 能捕获 await 的 reject，但捕获不到没有 await 的异步 Promise，链式调用要用 catch。

### 追问 8：基础：CSS 与布局
- 问题：你简历里提到 Flex/Grid、Sass/Less/SCSS 和 Element Plus。面试官问布局基础和移动端适配时，你会怎么结合项目回答？
- 考察维度：盒模型、BFC、Flex/Grid、移动端适配、动画性能
- 触发依据：当前追问点：基础：CSS 与布局。边界：只围绕前端实习高频 CSS 基础追问：盒模型、BFC、居中、三栏布局、Flex/Grid、选择器优先级、移动端适配、0.5px、动画和 transform 性能；不追问复杂图形算法或设计系统治理。
- 合格标准：合格回答需要覆盖背景、个人动作、技术选择、结果验证；追问可以继续，但必须严格控制在该追问点的逻辑边界内：只围绕前端实习高频 CSS 基础追问：盒模型、BFC、居中、三栏布局、Flex/Grid、选择器优先级、移动端适配、0.5px、动画和 transform 性能；不追问复杂图形算法或设计系统治理。
- 我的回答：CSS 基础我会从盒模型、布局和性能讲。标准盒模型 width 只包含 content，IE 盒模型包含 padding 和 border，所以项目里常用 box-sizing:border-box 降低计算复杂度。BFC 可以解决 margin 塌陷、清除浮动、阻止浮动覆盖。Flex 适合一维布局，比如导航和卡片行；Grid 适合二维布局，比如看板和商品瀑布区。移动端适配会结合 rem、vw、媒体查询和安全区。动画优先用 transform 和 opacity，因为它们更容易走合成层，减少 layout 和 paint。

### 追问 9：基础：浏览器存储与安全
- 问题：cookie、localStorage、sessionStorage 有什么区别？JWT 如果被劫持会怎样？XSS 和 CSRF 分别是什么，前端能怎么防？
- 考察维度：cookie、localStorage、sessionStorage、JWT、XSS、CSRF
- 触发依据：当前追问点：基础：浏览器存储与安全。边界：只围绕浏览器存储、登录态、cookie 属性、JWT 风险、XSS、CSRF、SameSite、HttpOnly、前端安全基础追问，不深入企业级风控系统。
- 合格标准：合格回答需要覆盖背景、个人动作、技术选择、结果验证；追问可以继续，但必须严格控制在该追问点的逻辑边界内：只围绕浏览器存储、登录态、cookie 属性、JWT 风险、XSS、CSRF、SameSite、HttpOnly、前端安全基础追问，不深入企业级风控系统。
- 我的回答：cookie 会随请求自动携带，可设置 HttpOnly、Secure、SameSite，适合服务端会话；localStorage 持久保存但容易受 XSS 影响，sessionStorage 随标签页生命周期结束。JWT 被劫持后攻击者可冒充用户，所以要缩短有效期、刷新机制、HttpOnly cookie 或严格 XSS 防护。XSS 是注入恶意脚本，前端要转义输出、避免 dangerouslySetInnerHTML、使用 CSP；CSRF 是利用 cookie 自动携带伪造请求，可以用 SameSite、CSRF Token、校验 Origin/Referer 防护。

### 追问 10：基础：Vue / React 框架
- 问题：你的简历同时有 Vue3、React、Pinia、Redux Toolkit。面试官如果比较 Vue 和 React 的状态更新模型，你会怎么说？
- 考察维度：响应式、组件通信、Hooks、Redux/Pinia、Diff/key
- 触发依据：当前追问点：基础：Vue / React 框架。边界：只围绕前端实习高频框架基础追问：Vue3 Composition API、响应式、生命周期、key、组件通信、Pinia；React Hooks、Redux Toolkit、路由、受控组件、Diff 基本思想；不追问框架源码细节到调度器完整实现。
- 合格标准：合格回答需要覆盖背景、个人动作、技术选择、结果验证；追问可以继续，但必须严格控制在该追问点的逻辑边界内：只围绕前端实习高频框架基础追问：Vue3 Composition API、响应式、生命周期、key、组件通信、Pinia；React Hooks、Redux Toolkit、路由、受控组件、Diff 基本思想；不追问框架源码细节到调度器完整实现。
- 我的回答：Vue3 用 Proxy 做响应式，相比 Vue2 的 Object.defineProperty 能更自然地代理对象新增删除和数组变化。React 更偏向状态变化触发重新渲染，再通过虚拟 DOM diff 更新 UI。列表 key 用于帮助框架识别节点身份，不建议用 index，因为插入删除会导致状态错位。Pinia 写法轻量、适合 Vue 组合式 API；Redux Toolkit 规范更强，适合 React 中复杂全局状态。Hooks 依赖数组容易闭包过期，我会优先保证语义正确，再用 useMemo/useCallback 解决真实性能问题。

### 追问 11：基础：工程化与性能
- 问题：你简历里写 Vite、TypeScript strict、ESLint flat config、自动导入和路由级代码分割。面试官问工程化时，你会怎么说明这些配置解决了什么问题？
- 考察维度：Vite/Webpack、代码分割、懒加载、质量规范、性能验证
- 触发依据：当前追问点：基础：工程化与性能。边界：只围绕前端实习高频工程化追问：Vite/Webpack 基本差异、loader/plugin 概念、代码分割、路由懒加载、Tree Shaking、ESLint/Prettier、构建体积、首屏性能；不要求候选人实现完整打包器。
- 合格标准：合格回答需要覆盖背景、个人动作、技术选择、结果验证；追问可以继续，但必须严格控制在该追问点的逻辑边界内：只围绕前端实习高频工程化追问：Vite/Webpack 基本差异、loader/plugin 概念、代码分割、路由懒加载、Tree Shaking、ESLint/Prettier、构建体积、首屏性能；不要求候选人实现完整打包器。
- 我的回答：Vite 开发快是因为利用浏览器原生 ESM，按需转换模块，生产构建再用 Rollup；Webpack 更偏整体依赖图打包。路由懒加载和动态 import 能把非首屏代码拆出去，但要处理 loading、错误边界和预加载策略。首屏白屏我会看网络瀑布、JS 包体积、CSS/字体阻塞、接口耗时、主线程长任务和组件渲染。TypeScript strict 提前暴露类型问题，ESLint/Prettier 保证团队风格和低级错误收敛，CI 里跑 lint/build 可以避免坏代码进入主干。

### 追问 12：基础：手写题与算法意识
- 问题：大厂前端实习经常会加手写题：防抖节流、Promise.all、深拷贝、数组 flat、并发控制，你会优先准备哪几个？每个核心思路是什么？
- 考察维度：防抖节流、Promise.all、深拷贝、并发控制、数组扁平化、基础算法
- 触发依据：当前追问点：基础：手写题与算法意识。边界：只围绕前端实习常见手写题和基础算法意识追问：防抖节流、Promise.all/race、深拷贝、flat、new、bind、并发控制、数组/字符串/树基础题，不要求复杂竞赛算法。
- 合格标准：合格回答需要覆盖背景、个人动作、技术选择、结果验证；追问可以继续，但必须严格控制在该追问点的逻辑边界内：只围绕前端实习常见手写题和基础算法意识追问：防抖节流、Promise.all/race、深拷贝、flat、new、bind、并发控制、数组/字符串/树基础题，不要求复杂竞赛算法。
- 我的回答：我会优先准备防抖、节流、Promise.all、深拷贝、并发控制和 flat。防抖适合搜索联想，等用户停止输入再发请求；节流适合滚动和 resize，固定间隔执行。Promise.all 要按原顺序收集结果，任意一个 reject 就整体 reject。深拷贝要考虑循环引用，可用 WeakMap 记录已拷贝对象，再处理 Date、RegExp、Map、Set。并发控制核心是维护任务队列和正在执行数量，完成一个就补下一个，保证同时最多 k 个请求。

### 追问 13：项目三：仿东方甄选电商
- 问题：仿东方甄选电商项目里，Pinia、路由懒加载和通用组件分别解决了什么问题？
- 考察维度：Vue 工程化、状态持久化、页面复用
- 触发依据：当前追问点：项目三：仿东方甄选电商。边界：只围绕 Vue3、Pinia、路由懒加载、状态持久化、商品/购物车/筛选页面、组件复用和布局适配追问，不扩展到支付、库存、订单履约等简历未体现的后端业务。
- 合格标准：合格回答需要覆盖背景、个人动作、技术选择、结果验证；追问可以继续，但必须严格控制在该追问点的逻辑边界内：只围绕 Vue3、Pinia、路由懒加载、状态持久化、商品/购物车/筛选页面、组件复用和布局适配追问，不扩展到支付、库存、订单履约等简历未体现的后端业务。
- 我的回答：这个项目里 Pinia 主要管理购物车、用户状态、筛选条件等跨组件状态；路由懒加载降低首屏包体积；通用组件如商品卡片、筛选栏、价格展示减少重复。持久化适合购物车和用户偏好，不适合临时弹窗、加载态和易过期的搜索结果。组件设计上我会用 props 传展示数据，用 emits 抛用户行为，复杂展示区域用插槽，不会为了复用把业务判断塞得过重。性能验证可以看打包产物、首屏请求数量和 Lighthouse 指标。

### 追问 14：工程化与反思
- 问题：如果让你再优化这份简历，你会先补强哪三类能力或项目表达？
- 考察维度：工程化能力、性能意识、架构复盘
- 触发依据：当前追问点：工程化与反思。边界：只围绕工程化配置、性能验证、架构复盘、AI 工具辅助开发和代码质量控制追问，不要求候选人编造没有实际做过的线上指标。
- 合格标准：合格回答需要覆盖背景、个人动作、技术选择、结果验证；追问可以继续，但必须严格控制在该追问点的逻辑边界内：只围绕工程化配置、性能验证、架构复盘、AI 工具辅助开发和代码质量控制追问，不要求候选人编造没有实际做过的线上指标。
- 我的回答：如果继续优化简历，我会补强三类表达：第一，把项目指标量化，比如首屏体积、渲染次数、异常率；第二，把技术取舍写清楚，比如为什么选 SSE、Pinia 或 Redux；第三，把复盘写成问题、方案、验证和沉淀，而不是只列技术栈。AI 工具辅助开发时，我不会直接接受代码，而会先读 diff、跑 lint/build、补边界测试，并用最小改动保证可维护性。

### 追问 15：实习经历
- 问题：你在 MindFlow 实习里最有代表性的一段工作是什么？如果只选一个改动说服面试官，你会选哪一个？
- 考察维度：业务理解、数据链路、协作落地
- 触发依据：当前追问点：实习经历。边界：只围绕 img2code 数据生产链路、标注质检工具、代码比对、状态流转、异常标记、数据清洗和质量校验追问，不扩展到公司内部不可公开实现或后端训练算法细节。
- 合格标准：合格回答需要覆盖背景、个人动作、技术选择、结果验证；追问可以继续，但必须严格控制在该追问点的逻辑边界内：只围绕 img2code 数据生产链路、标注质检工具、代码比对、状态流转、异常标记、数据清洗和质量校验追问，不扩展到公司内部不可公开实现或后端训练算法细节。
- 我的回答：MindFlow 实习里我会选 img2code 数据生产链路里的样本预览和代码比对工具来讲。前端负责把图片、生成代码、标注状态、异常原因组织成质检人员能高效判断的页面，并支持状态流转和异常标记。这个改动能体现我对业务流程、数据质量和协作效率的理解。遇到状态不一致时，我会先核对前端本地状态更新，再看接口返回和状态机设计，最后补充日志和异常提示，避免质检人员误判。

### 追问 16：基础：跨域与 SSE/WebSocket
- 问题：结合你的 AgentChat 项目，为什么 SSE 可以满足流式输出？如果面试官继续问 HTTP/1.1、HTTP/2、HTTP/3、HTTPS、缓存和跨域，你会怎么串起来回答？
- 考察维度：HTTP、HTTPS、TCP/UDP、缓存、跨域、SSE/WebSocket 场景取舍
- 触发依据：当前追问点：基础：跨域与 SSE/WebSocket。边界：只围绕前端实习高频网络基础追问：HTTP 各版本差异、HTTPS/TLS 基本流程、TCP/UDP 区别、三次握手四次挥手、浏览器缓存、跨域/CORS、SSE 与 WebSocket 取舍；不追问后端网关、服务端训练或复杂网络内核实现。
- 合格标准：合格回答需要覆盖背景、个人动作、技术选择、结果验证；追问可以继续，但必须严格控制在该追问点的逻辑边界内：只围绕前端实习高频网络基础追问：HTTP 各版本差异、HTTPS/TLS 基本流程、TCP/UDP 区别、三次握手四次挥手、浏览器缓存、跨域/CORS、SSE 与 WebSocket 取舍；不追问后端网关、服务端训练或复杂网络内核实现。
- 我的回答：跨域本质是浏览器同源策略限制脚本读取不同源响应。CORS 通过服务端返回 Access-Control-Allow-Origin 等响应头授权，复杂请求会先发 OPTIONS 预检，带 cookie 时还需要 credentials 和明确 origin。SSE 基于 HTTP 长连接，适合服务端持续推送文本流，天然支持断线重连，前端实现简单；WebSocket 是全双工，适合双向实时协作。AgentChat 主要是模型向前端持续输出，所以 SSE 更合适。

### 追问 17：项目二：仿网易云音乐
- 问题：仿网易云音乐这个项目里，Redux 根状态和底部播放器常驻布局是怎么配合的？
- 考察维度：React 架构、播放器状态、交互连续性
- 触发依据：当前追问点：项目二：仿网易云音乐。边界：只围绕 React、Redux Toolkit、Audio API、播放状态、路由布局、播放模式、评论交互和性能加载追问，不追问真实网易云版权、推荐算法或服务端音乐资源实现。
- 合格标准：合格回答需要覆盖背景、个人动作、技术选择、结果验证；追问可以继续，但必须严格控制在该追问点的逻辑边界内：只围绕 React、Redux Toolkit、Audio API、播放状态、路由布局、播放模式、评论交互和性能加载追问，不追问真实网易云版权、推荐算法或服务端音乐资源实现。
- 我的回答：仿网易云音乐里 Redux 根状态维护播放队列、当前歌曲、播放模式、播放进度和评论等信息，底部播放器常驻在路由布局外层，这样切换页面时 Audio 实例和 UI 不会被卸载。Audio 实例可以由播放器组件或自定义 hook 管理，Redux 存可序列化状态，真实 DOM 实例不放入 Redux。切歌、拖动进度、播放暂停由 UI dispatch 状态变化，同时监听 timeupdate、ended 等事件反向同步。代码分割用于降低首屏资源，播放核心组件保持稳定挂载。
